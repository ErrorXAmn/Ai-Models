[project]
name = "ai-models"
version = "1.0.0"
requires-python = ">=3.13"
dependencies = [
    "flask",
    "flask-cors",
    "httpx2"
]

[dependency-groups]
dev = [
    "workers-py",
    "workers-runtime-sdk"
]
